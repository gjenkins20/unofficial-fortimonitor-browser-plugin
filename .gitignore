# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/

# Node / build output (for future extension scaffold)
node_modules/
dist/
build/
*.log
npm-debug.log*

# Playwright (FMN-116 E2E suite)
test-results/
playwright-report/
.playwright-cache/
tests/e2e/.artifacts/
tests/e2e/.fixtures/

# Ollama-fixture leak guard (FMN-120 Phase 2). Older fixture builds
# could drop a model store at the repo root when OLLAMA_MODELS wasn't
# pinned. Belt-and-suspenders.
/qwen*/
/llama*/
/mistral*/
/gemma*/
/command*/

# Environment / secrets (defense in depth - nothing secret currently expected)
.env
.env.local
.env.*.local
!.env.example
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.der
credentials.json
auth.json
cookies.txt

# HAR / network capture files (may contain session tokens)
*.har
*.pcap
captures/

# Local Claude state and AI agent instructions - not for public distribution
.claude/
CLAUDE.md
.mcp.json

# Internal development files
TODO.md
agents/
/marketing/
docs/project_plan.md
docs/task_tracker.md
docs/qa_review.md

# OS
Thumbs.db
AGENT_BEHAVIOR.md
CLAU_Instructions.md

# FMN-135 local probe artifacts (creds + persistent profile + ad-hoc probe scripts)
fmn-135-*.mjs

# FMN-138 SSO Discovery captures (contain session cookies + IdP cert material)
tools/discovery/sso-capture-*.json

# FMN-194 Monitoring Policies discovery captures (contain session cookies)
tools/discovery/fmn-194-capture-*.json

# FMN-199 Templates create discovery captures (contain session cookies)
tools/discovery/fmn-199-capture-*.json

# FMN-151 live-FortiMonitor reproduction profile (cookies, local storage)
tests/e2e/.profile-fmn-live/
tests/e2e/.profile-fmn-live-cookies.json
# FMN-241 per-port profile dirs for parallel authenticated launcher instances
# (FMN_PROFILE_DIR=...). Contain session cookies - never commit.
tests/e2e/.profile-fmn-live-*/

# Live-spec artifacts (screenshots + JSON capture customer data from real tenant)
tests/e2e/__artifacts__/

# Foreign-tenant captures (FMN-211 Phase A) - hand-reviewed before sharing,
# never checked in.
tools/qa/fmn-211-foreign-tenant-capture.json

# --- 2026-07-20 (per operator request) ---
# Marketing image assets that churn on this machine (external re-encodes).
# NOTE: these paths are already TRACKED, so these entries do NOT stop
# `git status` from showing them as modified - .gitignore only affects
# untracked paths. No untracking was performed.
docs/marketing/frames/
docs/marketing/screenshots/
docs/marketing/social-preview.png

# Ad-hoc untracked ticket / probe artifacts (FMN-211/244/263/284/286/287) +
# the NAS mirror handoff note.
/NAS-MIRROR-HANDOFF.md
docs/api-discovery/metric-data.md
docs/mockups/fmn-244-training-submenu.html
tools/dev/fmn-286-login-probe.mjs
tools/dev/fmn-286-origin-diag.mjs
tools/dev/fmn-286-wizard-qa.mjs
tools/discovery/fmn-284-metric-shape.mjs
tools/discovery/fmn-287-metric-probe.mjs
tools/qa/fmn-211-scrape-foreign-tenant.py
tools/qa/fmn-263-generate-report.mjs
tools/qa/fmn-263-rebuild-pdf.mjs
tools/qa/out/
